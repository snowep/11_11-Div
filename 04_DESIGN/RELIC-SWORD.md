---
tags: [design, relic, product]
---

# RELIC: SWORD — Canon rules

**Object:** Type XV European longsword (realistic), single master vector.

**Fracture:** single, deliberate mid-blade void.
- **Baseline:** Y = 48.5% of master height (record in master vector).

**Distribution:**
- Upper half = tee (`TEE_PRINT`) only.
- Lower half = card (`CARD_PRINT`) only.
- No shard in canonical release.

**Found variant rules:** see decision note in [[LOCKED-DECISIONS]] if used.

**Exports (must exist):**
- `MASTER_FULL` (AI/SVG)
- `TEE_PRINT_SAFE` (upper half)
- `CARD_PRINT` (lower half)
- `OVERLAY_PROOF` (PNG @ 300dpi)
